
# Дослідження olk14msgsource

Файл **`1-599879.olk14msgsource`** — це **файл електронного повідомлення (email message source)**, який належить до **формату Microsoft Outlook для macOS (Outlook 2011 / 2016 / 2019)**.

Нижче — детальне пояснення його природи та значення в судово-технічному контексті 👇

---

## 🧩 **1. Походження файлу**

Файл із розширенням **`.olk14msgsource`** зберігається у **структурі даних профілю Microsoft Outlook для macOS** (версії 14.x — Outlook 2011, а іноді й 15/16).
Зазвичай такі файли містяться в каталозі:

```
~/Documents/Microsoft User Data/Office 2011 Identities/Main Identity/Data Records/Messages/
```

або

```
~/Library/Group Containers/UBF8T346G9.Office/Outlook/Outlook 15 Profiles/Main Profile/Data/Message/
```

---

## 📨 **2. Зміст і структура**

Файл **`.olk14msgsource`** — це **сирцеве тіло повідомлення електронної пошти**, у форматі **RFC 2822 / MIME**.
Він містить:

* **Заголовки (headers):**

  * `From`, `To`, `Cc`, `Subject`, `Date`, `Message-ID`, `Return-Path` тощо;
* **Основний текст повідомлення (body)**;
* **Кодування вкладень (attachments)** у вигляді Base64;
* Може також включати HTML-версію повідомлення.

Таким чином, він є **повною копією вихідного листа**, яку Outlook використовує для побудови інтерфейсу користувача.

---

## 🧠 **3. Значення у цифровій експертизі**

Під час комп’ютерно-технічного дослідження, файли `.olk14msgsource` мають **високу доказову цінність**, оскільки містять:

* повні метадані повідомлення (дата, адреси відправника/одержувача, маршрутизаційні поля);
* текст повідомлення;
* прикріплені файли у вихідному кодуванні;
* інформацію про сервера поштових хостів (через поля `Received:`);
* цифрові підписи (DKIM, SPF-заголовки).

У порівнянні з базою даних `.sqlitedb`, файл `.olk14msgsource` — це **окремий контейнер конкретного листа** (аналог `.eml` у Windows).

---

## 🧾 **4. Типове використання**

| Призначення              | Пояснення                                                                                                                |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------ |
| 📧 Зберігання листів     | Outlook створює `.olk14msgsource` для кожного отриманого або відправленого листа                                         |
| 🔍 Експертне дослідження | Файл може бути відкритий у текстовому редакторі або аналізований засобами типу `FTK`, `Autopsy`, `X-Ways`, `Mail Viewer` |
| 🧬 Відновлення листів    | При втраті бази даних Outlook (`Database.sqlite`), повідомлення можна реконструювати з `.olk14msgsource`                 |
| 🧠 Аналіз метаданих      | Дозволяє відновити хронологію комунікацій, справжній час відправки, маршрут SMTP тощо                                    |

---

## 🧮 **5. Формат і сумісність**

* Тип MIME: `text/plain` або `multipart/mixed`
* Відкривається:

  * Microsoft Outlook (macOS)
  * Mozilla Thunderbird
  * Apple Mail (через імпорт)
  * Текстові редактори (для аналізу вручну)
* Може бути конвертований у `.eml` або `.msg` для зручності експертизи.

---

## ⚖️ **6. Висновок (для експертного опису)**

> Файл `1-599879.olk14msgsource` є джерельним повідомленням електронної пошти, створеним клієнтом Microsoft Outlook для macOS (версії 14.x).
> Містить повну текстову структуру повідомлення згідно зі стандартом RFC 2822 (заголовки, тіло, вкладення, метадані).
> Використовується для зберігання та відображення електронної кореспонденції користувача.
> Має доказову цінність при дослідженні електронного листування, оскільки зберігає вихідний зміст повідомлення без змін.

---


# 🧩 **1. Призначення**

Файл `.olk14msgsource` містить *повний вихідний код електронного листа* (аналог `.eml`).
Він побудований за стандартом **RFC 2822 / MIME** і може бути розібраний стандартними бібліотеками Python або вручну.

---

# ⚙️ **2. Необхідні інструменти**

### Варіант 1 — автоматичний аналіз (рекомендовано)

Python ≥ 3.8
Модулі: `email`, `base64`, `os`

---

# 🧠 **3. Приклад коду для аналізу**

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-

import email
from email import policy
from email.parser import BytesParser
import base64
from pathlib import Path

# === Вкажи шлях до файлу .olk14msgsource ===
file_path = Path(r"D:\Evidence\1-599879.olk14msgsource")

# === Зчитування та парсинг ===
with open(file_path, 'rb') as f:
    msg = BytesParser(policy=policy.default).parse(f)

# === Метадані ===
print("=== МЕТАДАНІ ПОВІДОМЛЕННЯ ===")
print("Від:", msg.get('From'))
print("Кому:", msg.get('To'))
print("Копія:", msg.get('Cc'))
print("Тема:", msg.get('Subject'))
print("Дата:", msg.get('Date'))
print("Message-ID:", msg.get('Message-ID'))
print("-" * 60)

# === Основний текст ===
if msg.is_multipart():
    for part in msg.walk():
        ctype = part.get_content_type()
        if ctype == 'text/plain':
            print("=== ТЕКСТ ПОВІДОМЛЕННЯ ===")
            print(part.get_content())
        elif ctype == 'text/html':
            print("=== HTML-ВЕРСІЯ ===")
            print(part.get_content())
else:
    print("=== ТЕКСТ ПОВІДОМЛЕННЯ ===")
    print(msg.get_content())

# === Вкладення ===
attachments_dir = Path(file_path.parent) / "attachments"
attachments_dir.mkdir(exist_ok=True)

for part in msg.walk():
    if part.get_content_disposition() == 'attachment':
        filename = part.get_filename()
        if filename:
            filepath = attachments_dir / filename
            with open(filepath, 'wb') as f:
                f.write(part.get_payload(decode=True))
            print(f"Вкладення збережено: {filepath}")
```

---

# 📊 **4. Що отримаєш після виконання**

### В консолі:

```
=== МЕТАДАНІ ПОВІДОМЛЕННЯ ===
Від: Ivan Petrenko <ivan.petrenko@example.com>
Кому: Support <support@company.com>
Копія: 
Тема: Звіт за жовтень
Дата: Fri, 6 Nov 2025 14:33:21 +0200
Message-ID: <CAAA7@mail.example.com>
------------------------------------------------------------
=== ТЕКСТ ПОВІДОМЛЕННЯ ===
Доброго дня!
Надсилаю звіт за жовтень у вкладенні.
З повагою, Іван Петренко
```

### У теці `attachments/`:

```
report_october.xlsx
signature_logo.png
```

---

# 🧩 **5. Альтернатива — ручний перегляд**

Файл `.olk14msgsource` — звичайний текстовий документ, тож його можна відкрити у:

* **Notepad++**, **VS Code**, **Sublime Text**;
* пошукати ключові поля:

  ```
  From:
  To:
  Subject:
  Date:
  Message-ID:
  Content-Type:
  ```
* Якщо є вкладення — вони виглядають як великі Base64 блоки:

  ```
  Content-Disposition: attachment; filename="invoice.pdf"
  Content-Transfer-Encoding: base64
  JVBERi0xLjQKJ....(довгий набір символів)
  ```

  → їх можна декодувати вручну через онлайн-декодери або Python.

---

# ⚖️ **6. Для експертного висновку**

> Під час аналізу файлу `1-599879.olk14msgsource` слід виділити:
>
> * електронні адреси відправника та одержувачів;
> * точний час ідентифікації дати відправлення (`Date` + часова зона);
> * унікальний ідентифікатор листа (`Message-ID`);
> * зміст тексту повідомлення;
> * наявність та тип вкладень (MIME type, назва, розмір);
> * якщо присутні заголовки `Received:`, вони дозволяють відновити маршрут SMTP-передачі листа.

---




# Дослідження файлів SQLite (.sqlitedb)

Файли з розширенням *.sqlitedb - це бази даних SQLite, які часто зустрічаються в мобільних додатках (особливо iOS) та інших програмах. 

Покроковий підхід до їх експертного дослідження:

## 1. Попередній огляд та фіксація

**Створіть хеш-суми** файлів перед дослідженням (MD5, SHA-1, SHA-256) для підтвердження цілісності доказів.

## 2. Інструменти для дослідження

**Основні інструменти:**
- **DB Browser for SQLite** (SQLiteBrowser) - безкоштовний графічний інтерфейс
- **SQLite Expert** - професійний інструмент з додатковими функціями
- **SQLite командний рядок** - стандартна утиліта sqlite3

**Криміналістичні інструменти:**
- **Autopsy/Sleuth Kit** - включає модулі для SQLite
- **Belkasoft Evidence Center** - аналіз мобільних даних
- **Oxygen Forensic Detective** - для мобільної криміналістики
- **AXIOM** (Magnet Forensics)

## 3. Процес дослідження

### Перевірка цілісності бази
```bash
sqlite3 filename.sqlitedb "PRAGMA integrity_check;"
```

### Отримання структури бази
```sql
.schema  -- переглянути всі таблиці та їх структуру
.tables  -- список таблиць
```

### Аналіз метаданих
- Дата створення/модифікації файлу
- Версія SQLite
- Кодування тексту
- Журнали транзакцій (WAL файли)

### Витяг даних
Експортуйте дані у форматі CSV або JSON для подальшого аналізу та включення до висновку експерта.

## 4. Важливі аспекти для експертизи

**Шукайте:**
- Видалені записи (можливе відновлення через unallocated space)
- Timestamps (часові мітки активності)
- Геолокаційні дані
- Переписку (для месенджерів)
- Історію активності

**Звертайте увагу на:**
- Таблиці з префіксами "deleted_" або "backup_"
- Поля з binary data (BLOB) - можуть містити зображення
- Зв'язки між таблицями (foreign keys)

## 5. Документування

- Назва та хеш-сума досліджуваних файлів
- Використані інструменти та їх версії
- Методику дослідження
- Знімки екрану структури бази
- Результати SQL-запитів
- Таблиці з витягнутими даними

## 6. Відновлення видалених даних

Використовуйте спеціалізовані інструменти:
- SQLite Forensic Explorer
- Undark (інструмент для відновлення)


